# Spring Boot configuration
spring.application.name=ms-core-app

# Database configuration (PostgreSQL)
spring.datasource.url=jdbc:postgresql://localhost:5432/pos_db
spring.datasource.username=andrewramirez
spring.datasource.password=sul4m1995*
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate/JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server configuration
server.port=8083

# Swagger UI and OpenAPI configuration
springdoc.swagger-ui.path=/swagger-ui
springdoc.api-docs.path=/openapi
springdoc.swagger-ui.operations-sorter=alpha

# Logging
logging.level.root=INFO
logging.level.com.suresell.mscoreapp=DEBUG
